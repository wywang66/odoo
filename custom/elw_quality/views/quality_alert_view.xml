<odoo>
    <data>
        <record model="ir.ui.view" id="elw_quality_alert_search_view">
            <field name="name">elw.quality.alert.search.view</field>
            <field name="model">elw.quality.alert</field>
            <field name="arch" type="xml">
                <search string="Quality Alert Search ">
                    <field name="name" string="Quality Alert"
                           filter_domain="[                     '|', '|', '|', ('name', 'ilike', self), ('product_id', 'ilike', self), ('product_tmpl_id', 'ilike', self), ('lot_ids', 'ilike', self)]"/>
                    <field name="product_id" string="Product"
                           filter_domain="[                     '|', '|', ('product_id', 'ilike', self), ('product_tmpl_id', 'ilike', self), ('lot_ids', 'ilike', self)]"/>
                    <field name="tag_ids"/>
                    <field name="user_id"/>
                    <field name="team_id"/>
                    <filter string="My Alerts" name="myalerts" domain="[('user_id', '=', uid)]"/>
                    <separator/>
                    <filter string="In Progress" name="inprogress" domain="[('stage_id.done', '=', False)]"/>
                    <filter string="Done" name="done" domain="[('stage_id.done', '=', True)]"/>
                    <filter string="Unsolved TopPriority" name="unsolved_top_priority"
                            domain="[('priority', '=', '3'),('stage_id.done', '=', False) ]"/>
                    <separator/>
                    <filter name="filter_date_assign" date="date_assign"/>
                    <filter name="filter_date_close" date="date_close"/>
                    <separator/>
                    <filter string="Late Activities" name="activities_overdue"
                            domain="[('my_activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                            help="Show all records which has next action date is before today"/>
                    <filter string="Today Activities" name="activities_today"
                            domain="[('my_activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                    <filter string="Future Activities" name="activities_upcoming_all"
                            domain="[('my_activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                    <group expand="0" string="Group By">
                        <filter string="Stage" name="groupby_stage" domain="[]" context="{'group_by': 'stage_id'}"/>
                        <filter string="Responsible" name="groupby_user" domain="[]" context="{'group_by': 'user_id'}"/>
                        <filter string="Root Cause" name="groupby_reason" domain="[]"
                                context="{'group_by': 'reason_id'}"/>
                        <filter string="Creation Date" name="groupby_createmonth" domain="[]"
                                context="{'group_by': 'create_date'}"/>
                        <filter string="Quality Team" name="groupby_team_id" domain="[]"
                                context="{'group_by': 'team_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <record id="elw_quality_alert_graph_view" model="ir.ui.view">
            <field name="name">elw.quality.alert.graph.view</field>
            <field name="model">elw.quality.alert</field>
            <field name="arch" type="xml">
                <graph string="Quality Alert Analysis" sample="1">
                    <field name="reason_id"/>
                    <field name="stage_id"/>
                </graph>
            </field>
        </record>

        <record model="ir.ui.view" id="elw_quality_alert_kanban_view">
            <field name="name">elw.quality.alert.kanban.view</field>
            <field name="model">elw.quality.alert</field>
            <field name="arch" type="xml">
                <kanban default_group_by="stage_id" class="o_kanban_small_column" sample="1">
                    <field name="description"/>
                    <field name="user_id"/>
                    <field name="stage_id"/>
                    <field name="tag_ids"/>
                    <field name="priority"/>
                    <field name="activity_ids"/>
                    <field name="activity_state"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                                <div>
                                    <field name="tag_ids"/>
                                </div>
                                <div>
                                    <strong>
                                        <field name="product_id"/>
                                    </strong>
                                </div>
                                <div>
                                    <field name="display_name"/>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <field name="priority" widget="priority"/>
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <field name="user_id" widget="many2one_avatar_user"/>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <record model="ir.ui.view" id="elw_quality_alert_tree_view">
            <field name="name">elw.quality.alert.tree.view</field>
            <field name="model">elw.quality.alert</field>
            <field name="arch" type="xml">
                <tree sample="1" string="Quality Alert Tree ">
                    <field name="name" decoration-bf="1"/>
                    <!--                    <field name="product_tmpl_id" optional="show"/>-->
                    <field name="product_id" optional="show"/>
                    <field name="lot_ids" optional="hide"/>
                    <field name="team_id" optional="show"/>
                    <field name="user_id" optional="show" widget="many2one_avatar_user"/>
                    <field name="check_id" optional="show"/>
                    <field name="stage_id" optional="show" widget="badge"
                           decoration-success="stage_id == 4"
                           decoration-info="stage_id == 3"
                           decoration-warning="stage_id == 2"
                           decoration-danger="stage_id ==1 "/>
                    <field name="tag_ids" optional="hide" widget="many2many_tags"/>
                    <field name="reason_id" optional="hide"/>
                    <field name="priority" optional="hide"/>
                    <field name="date_assign" optional="show"/>
                    <field name="partner_id" optional="hide"/>
                </tree>
            </field>
        </record>

        <record model="ir.ui.view" id="elw_quality_alert_form_view">
            <field name="name">elw.quality.alert.form.view</field>
            <field name="model">elw.quality.alert</field>
            <field name="arch" type="xml">
                <form string="Quality Alert Form">
                    <header>
                        <field name="stage_id" widget="statusbar" options="{'clickable': '1'}"/>
                    </header>
                    <sheet>
                        <field name="company_id" invisible="1"/>
                        <field name="product_id" invisible="1"/>
                        <div class="oe_button_box" name="button_box">
                            <button name="action_see_check" type="object" invisible="not check_id"
                                    class="oe_stat_button"
                                    icon="fa-check-square-o">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_value">
                                        <field name="check_id" readonly="1"/>
                                    </span>
                                    <span class="o_stat_text">Quality Check</span>
                                </div>
                            </button>
                        </div>
                        <div class="oe_title">
                            <h3>
                                <field name="name" readonly="1"/>
                            </h3>
                        </div>
                        <group>
                            <group>
                                <field name="active" invisible="1"/>
                                <field name="title"/>
                                <field name="product_tmpl_id" invisible="1"/>
                                <!--         group_product_variant is a bool  -->
                                <field name="product_id"/>
                                <!-- product.group_product_variant makes product_id not selectable"/>-->
                                <!-- <field name="product_id" groups="product.group_product_variant"/>-->
                                <field name="picking_id"/>
                                <field name="check_id" options="{'no_create': True}"/>
                                <field name="point_id"/>
                                <field name="lot_info" invisible="picking_id"/>
                                <field name="has_lot_id" invisible="1"/>
                                <field name="picking_code" invisible="1"/>
                                <field name="lot_ids" widget="many2many_tags"
                                       context="{'default_product_id': product_id}" groups="stock.group_production_lot"
                                       invisible="picking_code != 'outgoing' or not has_lot_id" required="has_lot_id" force_save="1"/>
                                <field name="lot_name"
                                       context="{'default_product_id': product_id}" groups="stock.group_production_lot"
                                       invisible="picking_code != 'incoming' or not has_lot_id" required="has_lot_id" force_save="1"/>
                            </group>
                            <group>
                                <field name="team_id"/>
                                <field name="user_id" domain="[('share', '=', False)]"/>
                                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                <field name="reason_id"/>
                                <field name="priority" widget="priority"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Description" name="description">
                                <field name="description" placeholder="Description of the issue..."/>
                            </page>
                            <page string="Corrective Actions" name="corrective_actions">
                                <field name="action_corrective"
                                       placeholder="Describe the corrective actions you did..."/>
                            </page>
                            <page string="Preventive Actions" name="preventive_actions">
                                <field name="action_preventive"
                                       placeholder="Describe the preventive actions you did..."/>
                            </page>
                            <page string="Miscellaneous" name="miscellaneous">
                                <group>
                                    <group>
                                        <field name="partner_id"/>
                                        <field name="company_id" groups="base.group_multi_company"/>
                                        <field name="email_cc" groups="base.group_no_one"/>
                                    </group>
                                    <group>
                                        <field name="date_assign"/>
                                        <field name="date_close" invisible="not date_close"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <!-- actions opening views on models -->
        <record model="ir.actions.act_window" id="elw_quality_alert_action_window">
            <field name="name">Quality Alert</field>
            <field name="res_model">elw.quality.alert</field>
            <field name="view_mode">kanban,tree,form</field>
        </record>

        <record model="ir.actions.act_window" id="elw_quality_alert_action_window_reporting">
            <field name="name">Quality Alert</field>
            <field name="res_model">elw.quality.alert</field>
            <field name="view_mode">graph</field>
        </record>

    </data>
</odoo>
